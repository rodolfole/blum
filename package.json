{
  "name": "blum",
  "version": "1.0.0",
  "description": "Luxury perfume landing page for Blum — scroll-scrub hero, glassmorphic cart drawer, performance-tuned for mobile.",
  "license": "ISC",
  "type": "commonjs",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "tailwindcss -i input.css -o assets/tailwind.css --minify",
    "watch": "tailwindcss -i input.css -o assets/tailwind.css --watch",
    "dev": "pnpm run watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
